<input ${py.attrs(attrs)} 
		type="checkbox"
		id="${filter_id}"
		name="${filter_id}"
		class="checkbox grid-domain-selector" 
		onclick="do_filter();" 
		value="${filter_domain}"
		title="${help}">
	${text_val}
</input>
